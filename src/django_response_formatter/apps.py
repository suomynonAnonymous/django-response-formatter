"""Django app configuration for django_response_formatter."""

from django.apps import AppConfig


class DjangoResponseFormatterConfig(AppConfig):
    """App config for the Django Response Formatter package."""

    name = "django_response_formatter"
    verbose_name = "Django Response Formatter"
    default_auto_field = "django.db.models.BigAutoField"
